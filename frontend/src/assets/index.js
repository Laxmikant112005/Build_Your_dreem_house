/**
 * Assets Index
 * 
 * This folder contains static assets used throughout the application.
 * 
 * Asset Types:
 * - Images (logos, icons, backgrounds)
 * - Fonts (custom font files)
 * - Videos (promotional, tutorials)
 * - Documents (PDFs, guides)
 * - Data (JSON files, mock data)
 * 
 * Best Practices:
 * - Use WebP/AVIF for images when possible
 * - Lazy load large assets
 * - Use CDN for large media files
 * - Optimize images before adding
 * 
 * Note: For React, you can import assets directly:
 * import logo from './assets/logo.png';
 * 
 * Or use the URL import pattern:
 * const logoUrl = new URL('./assets/logo.png', import.meta.url).href;
 */

// Export asset paths if needed
// Typically, assets are imported directly in components

// Example: export const ASSETS = {
//   logo: './logo.png',
//   placeholder: './placeholder.png',
// };

