


/*
 * Deprecated: keep this module for backward compatibility.
 * Many services used to do `import api from './api'`.
 *
 * Now the real axios instance lives in `./axios`.
 */

import api from './axios';

export default api;

